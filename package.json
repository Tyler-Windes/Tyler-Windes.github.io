{
  "name": "tyler-windes-github-pages",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static professional portfolio for Tyler Windes.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "npm run build && node scripts/validate-public.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs --root dist --port 4173",
    "preview": "node scripts/serve.mjs --root dist --port 4173"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
